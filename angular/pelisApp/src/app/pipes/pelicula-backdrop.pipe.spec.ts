import { PeliculaBackdropPipe } from './pelicula-backdrop.pipe';

describe('PeliculaBackdropPipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculaBackdropPipe();
    expect(pipe).toBeTruthy();
  });
});
